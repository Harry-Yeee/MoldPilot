# MoldPilot's Postgres moved to the shared LJ_ERP infrastructure (2026-07-17).
# This stub includes the root compose so existing scripts (pnpm db:up / db:down,
# pilot:* preflight) keep working unchanged from this directory. Container and
# volume names are pinned in the root file, so this manages the SAME container.
# Edit ../docker-compose.yml — not this file. (Previous version: git history.)
include:
  - ../docker-compose.yml
